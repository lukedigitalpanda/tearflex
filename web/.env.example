# Server-only base URL of the Django API, read by the BFF route handlers.
# Never exposed to the browser (not NEXT_PUBLIC_*) — the client only talks to
# the same-origin BFF, which attaches the httpOnly-cookie token server-side.
API_URL=http://localhost:8000/api
